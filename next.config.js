/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // @todo enable for hosting on github.io domain, not onlydans.lol
  // basePath: process.env.NEXT_PUBLIC_BASE_PATH,
  // assetPrefix: process.env.NEXT_PUBLIC_BASE_PATH,
};

module.exports = nextConfig;
